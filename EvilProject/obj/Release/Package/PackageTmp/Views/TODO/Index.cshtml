@model IEnumerable<EvilProject.Models.TODO>

@{
    ViewBag.Title = "Index";
}

<div class="post">
	<h1 class="title"><a href="#">Lista TODO</a></h1>
	<p class="byline"><small>  @Html.ActionLink("Dodaj nowy", "Create",null, new { @class = "addlinksmall" })</small></p>
	<div class="entry">

<p>

<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.project_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.add_date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.project_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.add_date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id },new { @class="editlinksmall" })
            @Html.ActionLink("Delete", "Delete", new { id=item.Id },new { @class="deletelinksmall" })
        </td>
    </tr>
}

</table>
        </div>
  </div>

